// 菜单配置
// headerMenuConfig：头部导航配置
// asideMenuConfig：侧边导航配置

const headerMenuConfig = [];

// 配置中的icon不用理会，iceworks自动生成，但不会使用
const asideMenuConfig = [
  {
    path: '/blank',
    name: 'Blank',
    icon: 'blank',
  },
];

export { headerMenuConfig, asideMenuConfig };
